# OpenLabStock 成熟方案对比与产品取舍

仓库结构最近核对：2026-09-02；领域资料最近核对：2026-08-14。项目热度仅用于判断社区成熟度，不代表功能一定适合实验室。

## 参考系统

| 系统 | 开源情况与社区 | 适合借鉴的能力 | 不直接采用的原因 |
| --- | --- | --- | --- |
| [InvenTree](https://github.com/inventree/InvenTree) | MIT，约 7.3k GitHub Stars | 物料分类、最小库存、库位、库存追踪、导入 | 功能面更宽，批次、供应链和零件体系对当前组内流程偏重 |
| [Snipe-IT](https://github.com/grokability/snipe-it) | AGPL-3.0，约 14.8k Stars | 用户、权限、耗材领用、成员库存、导入和审计思路 | 核心是 IT 资产管理，不是双向库存流水系统 |
| [ERPNext](https://github.com/frappe/erpnext) | GPL-3.0，约 37.9k Stars | Material Receipt、Material Issue、Material Transfer、多仓库和可追溯流水 | 同时带采购、会计、销售和制造体系，部署与维护成本明显过高 |
| [Odoo](https://github.com/odoo/odoo) | Community/Enterprise 组合，约 53.6k Stars | 补货规则、仓库作业和完整业务工作流 | 模块和权限体系面向企业 ERP，超出实验组实际需求 |
| [Part-DB](https://github.com/Part-DB/Part-DB-server) | AGPL-3.0，约 1.7k Stars | 电子元件分类、库位和零件属性 | 领域偏电子元件，不适合作为通用实验耗材工作台 |
| [SmartStock / LabStock](https://github.com/rivkaperets07/LabStock) | 无许可证、单日 5 次提交的课程项目 | 把耗材、试剂、设备和样品拆为独立领域；效期、校准周期和样品滞留提醒表达直接 | H2 使用 `ddl-auto=create`，无认证/权限/流水/生产部署，前端写死 localhost；编译产物也进入仓库，不能作为生产实现基线 |

本次核对的官方资料：[InvenTree Stock 文档](https://docs.inventree.org/en/stable/stock/)、[Snipe-IT 文档](https://snipe-it.readme.io/docs/overview)、[ERPNext Stock 文档](https://docs.frappe.io/erpnext/user/manual/en/stock) 和 [Odoo Inventory](https://www.odoo.com/app/inventory)。GitHub 页面当前显示的热度只用于判断社区成熟度，不作为选型的唯一标准。

SmartStock 的价值主要是产品边界提示：效期、校准和样品处理时效不应伪装成普通耗材状态。若这些需求在 OpenLabStock 中重复出现，应作为耗材批次或独立资产/样品领域的可选能力引入，而不是给所有耗材增加一组空字段。其当前实现属于演示级 CRUD，不能替代本项目已有的持久化、权限、审计、迁移和部署边界。

## 状态化库存与物理单元先例

| 成熟做法 | 官方资料 | OpenLabStock 的对应实现 |
| --- | --- | --- |
| 批次追踪一组物品、序列号追踪唯一单件 | [Odoo Product tracking](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/product_management/product_tracking.html) | 库存单元可选择批次或序列 / 单件，同一耗材档案下保留多个不可变单元 UUID |
| 物理盒或包装是独立对象，不等于产品型号 | [Odoo Packages](https://www.odoo.com/documentation/19.0/applications/inventory_and_mrp/inventory/product_management/configure/package.html) | 盒 / 容器作为库存单元，盒号可读、二维码绑定 UUID，空盒归档后不重分配二维码 |
| 库存交易显式关联批次和序列明细 | [ERPNext Serial and Batch Bundle](https://docs.frappe.io/erpnext/serial-and-batch-bundle) | 出入库流水保存库存单元、位置、状态、使用范围和自用成员快照，不依赖备注拼接关系 |
| 库存项有状态、自定义状态代码和追踪历史 | [InvenTree Stock Status](https://docs.inventree.org/en/stable/stock/status/)、[Stock Tracking](https://docs.inventree.org/en/stable/stock/tracking/) | 每种耗材可自定义显示状态，底层保留可用 / 暂不可用 / 终止不可用语义，变更写入不可删除事件 |
| 库存可归属用户或组织 | [InvenTree Stock Ownership](https://docs.inventree.org/en/stable/stock/owner/) | 当前只保留实验室需要的开放使用 / 成员自用，自用绑定成员 UUID |
| 当前状态与历史动作分开记录 | [Snipe-IT Checkin/Checkout 与 Status Labels](https://snipe-it.readme.io/docs/overview)、[InvenTree Stock Tracking](https://docs.inventree.org/en/stable/stock/tracking/) | 状态回答“现在是否可用”，使用事件回答“谁在何时用过”；已启用探针可反复登记使用而不伪造状态变化或减少库存 |
| 字段名称、帮助文本和业务数据分离 | [Material 3 Text fields](https://m3.material.io/components/text-fields/guidelines)、[Google Forms](https://support.google.com/docs/answer/2839588) | 管理员按耗材配置格位编号和用途 / 实验项目说明；说明始终显示在输入框下方，placeholder 只给短示例，标准字段与流水语义不变 |

这些先例证明“型号、物理单元、当前明细”应是关联实体，而不是把盒号、位置、状态和姓名拼进一个备注字段。OpenLabStock 刻意不复制仓库拣货、生产工单、销售和财务模块。

### 可复用物品的精简使用流程

Snipe-IT 的 checkout/checkin 用于表达排他占用：资产借给一人后，在归还前不能再次借给别人。这适合电脑和仪器，但不适合仍放在公共探针盒中、可由多人先后使用的探针。InvenTree 的 Stock Tracking 会自动记录库存调整及操作人、时间和数量，适合作为追加式历史先例，但“使用后仍在库存中”不是一次库存数量调整。

因此 OpenLabStock 不把“已启用 → 已启用”包装成虚假状态变更，也不要求每次使用都做借出、归还或预订：

1. 可用明细的主操作是“登记使用”，一次提交记录操作人、发生时组织、时间、使用位置 / 项目和备注，库存数量不变。
2. 全新明细第一次登记使用时，在同一事务中自动转为默认“已启用”；用户不需要再提交一次状态变更。
3. 已启用明细后续使用只追加使用事件，A、B 可以先后留下各自记录，当前状态仍为已启用。
4. “成员自用”仍是访问边界，不等于最近使用人；B 不能使用 A 的自用明细，需先重新开放或由管理员转交。
5. “领出库存（数量减少）”保留为低频显式操作，避免日常登记误减可复用库存。
6. 误登记不删除历史，由本人或系统管理员追加整笔使用更正；更正不倒退当前状态，避免覆盖后来真实发生的变化。

### 自定义字段与登记模板取舍

成熟产品通常区分字段定义、帮助文字、校验和历史数据。任意表单生成器还必须同时处理字段类型、必填、选项、权限、历史快照、导出、停用和迁移；只在前端增加自由文本框会制造无法统计和无法审计的数据。

当前阶段采用受控配置：库存管理员可按耗材维护格位编号和用途 / 实验项目说明，系统仍使用固定字段与后端校验。若至少三类耗材反复需要相同的一组额外字段，再引入可复用“登记模板”，模板必须绑定稳定 UUID，不直接绑定可改名的分类文本。真正自定义字段只在明确了字段类型、必填、选项、权限、历史快照与导出口径后实施，不复制 Odoo Studio 的完整复杂度。

## 当前版本已经保留的成熟做法

- 出入库流水追加记录，当前库存由变动产生，不直接在列表中随意改数。
- 所有启用成员可查看完整库存和流水，成员与管理员的修改权限仍按身份分级。
- 每种耗材独立设置安全库存线。
- 账号无需邮箱，管理员可以重置密码、停用、删除和调整成员信息。
- Excel 导入按“导入后的当前库存”生成差额流水，并在写入前整批校验。
- 库存统计和流水支持导出。
- 桌面端与手机端使用同一套 Material 3 组件和交互状态。

## 本轮审计后已改进

- 不再把瓶、盒、支等不同单位直接相加；总览改为耗材种类、预警种类和流水笔数。
- 趋势图按每月入库/出库记录笔数统计，不再混加不同单位的数量。
- 成员弹窗提供统一的姓名、角色、分组表单和底部保存按钮。
- 原生下拉框和输入建议统一为 Material 菜单，支持选中反馈、动画和键盘操作。
- 手机筛选栏重新排版，并修复筛选后隐藏状态被卡片样式覆盖的问题。
- Excel 导入增加覆盖语义提示、重复行和逐行字段校验。
- 增加登录尝试限速、安全响应头和生产 HTTPS Cookie 配置。
- 有库存时禁止直接或通过 Excel 更换计量单位，避免当前数量与历史流水失去可比性。
- 修改自己的密码后保留当前设备并注销其他会话；CSV 导出会转义潜在公式单元格。
- 删除成员和分组使用站内 Material 确认对话框，不再调用浏览器原生弹窗。
- 正式数据迁移到 SQLite；写请求使用跨进程事务，登录会话也持久化到数据库。
- 增加跨平台的 SQLite 在线快照、完整性校验和保留期清理命令。
- 增加唯一组织分组与多选成员标签：课题组、公共平台等内部组织用于消费核算，职责标签只用于成员目录检索，避免一笔出库因多标签重复统计。
- 库存统计增加组织分组概览和分组耗材消耗，按流水发生时的组织快照统计，并排除 Excel 库存调整。
- 增加普通数量、按状态统计和按库存单元追踪三种模式；盒、批次、序列及盒内位置拥有结构化字段和二维码。
- 自用库存绑定成员 UUID；同一盒可同时包含开放和自用明细，安全库存只统计开放且可用数量。
- 状态显示名称可由库存管理员维护，但有库存时不能静默改变可用性语义；损坏、耗尽等终止原因统一写入处置备注。
- 可复用库存新增独立使用事件；首次使用可自动启用，重复使用不减库存，使用更正采用追加冲销事件。
- 批次级库存单元支持有效期、30 天临期提醒和过期处置边界；过期数量保留在总库存中，但不计入开放可用库存和安全库存判断。

## 组织分组与标签取舍

- [ERPNext Accounting Dimensions](https://docs.frappe.io/erpnext/accounting-dimensions) 将 Department、Branch、Business Unit 等作为交易维度写入交易并用于报表筛选；旧交易在维度停用后仍保留原值。这对应本系统把内部组织分组作为流水发生时的唯一组织快照。
- [Snipe-IT Managing Users](https://snipe-it.readme.io/docs/managing-users) 建议用 Groups 管理权限。本系统不把成员职责标签当作权限组，权限仍由系统所有者、系统管理员、库存管理员和普通成员四种身份决定。
- 采购负责人、危化品、光刻等标签允许多选，适合目录展示和交叉检索；一位成员可能同时拥有多个标签，因此标签不能直接作为库存消耗求和维度。
- 若未来确实要看“危化品相关成员共领用了什么”，只能明确标为可重叠分析，并提示各标签合计不可再相加；当前正式统计保持唯一组织分组，避免重复计算。

结论：保留组织分组，不能用标签替代。课题组、公共平台等属于组织分组；标签是补充信息，不是核算口径。

## 建议的后续优先级

### 稳定版发布前优先

1. 完成代码、品牌和第三方权利链复核，定稿并启用贡献许可流程；稳定版发布前启用分支规则与 GitHub Private Vulnerability Reporting。
2. 在全新 Linux 与隔离 Docker 环境完成安装、旧库升级、备份、恢复、回滚和容器烟测，确认示例部署不依赖内部实例配置。
3. 为登录、扫码定位、登记确认和库存写入建立最小浏览器回归，并在真实 Android/iOS 设备验收摄像头、PWA 与输入法边界。
4. 将现有备份命令接入实例自己的定时任务，并保持定期恢复演练；公共仓库只提供通用示例，不保存实例拓扑或运维记录。

### 有真实需求再做

- 多房间或多柜子需要独立盘点时，在现有库存单元之上增加库位词表和调拨任务；当前位置编号已可表达盒内格位，但不等于多仓库核算。
- 化学试剂可直接在批次库存单元上填写结构化有效期；系统按实时日期提供 30 天临期提醒，并在过期后阻止使用和出库。开封日期仍留待有明确流程时再建模。
- 现有探针在线表需要正式迁移时，设计一次性的库存单元快照导入和校验报告；不能复用只接受普通数量的当前库存回导表。
- 采购流程固定后增加补货申请或采购单关联。
- 消耗量足够多后再增加按成员或项目的细分报表；现有版本已提供按组织分组、耗材汇总的出库统计。

### 当前不建议加入

- 财务估值、销售、制造、审批流等 ERP 模块。
- 没有实际使用场景的多租户和多仓库入口。
- 为每个普通耗材启用序列号级追踪。
- 尚未实现但提前放在界面上的占位按钮。

## 结论

OpenLabStock 不需要复制一套完整 ERP。当前最合适的方向是继续保持轻量，把 InvenTree 的库存严谨性、Snipe-IT 的成员管理和 ERPNext 的可追溯流水原则缩减到实验组真正会重复使用的流程中。

当前 SQLite 方案适合单台服务器上的组内使用，并支持多个 Node 进程安全访问同一份本地数据。只有多台应用服务器、多实验室、外部系统集成或高可用需求出现后再迁移 PostgreSQL。
