# OpenLabStock AI 开发与设计指引

本文件是 AI 或新维护者进入 `openlabstock/` 后的第一阅读入口。修改前同时阅读：

- `docs/BUILD_ARCHITECTURE.md`
- `docs/ENGINEERING_WORKFLOW.md`
- `docs/REPOSITORY_GOVERNANCE.md`
- `README.md`
- `TODO.md`，先确认是否存在尚未交付的用户问题；新问题插入时不得遗忘旧事项。

## 1. 项目边界

- OpenLabStock 是独立项目，只修改当前仓库内文件。不得假定同级目录、私有运营仓库或聊天记录属于本项目上下文。
- 本地默认地址为 `http://127.0.0.1:4388`；不要占用其他项目端口。
- 前端由 Astro 构建到 `dist/`，Node API 在 `server.mjs`，SQLite 数据层在 `storage.mjs`。
- 通用 systemd 示例使用 `/opt/openlabstock`、`/var/lib/openlabstock` 和 `openlabstock.service`。发布包绝不能包含或覆盖任何实例的数据库。
- 项目名称、新部署标识和内部协议统一使用 `OpenLabStock` / `openlabstock`。实例自定义显示名称不得进入协议、数据库关联或权限判断。

### 仓库与信息边界

- 当前仓库是 OpenLabStock 通用应用代码、迁移和测试的唯一来源。不得长期维护“私有版应用”和“开源版应用”两套分叉源码；私有运营仓库只能引用本仓库的确切版本、标签或镜像。
- 公共应用文档不得写入真实服务器 IP、SSH 用户、共用站点拓扑、客户信息、价格合同、CLA 签署记录、未公开安全事件、数据库、备份、日志、凭据或内部聊天记录。
- AI 不得把私有运营内容复制、摘要或改写到公共应用仓库，除非维护者明确批准且内容已经去标识化。私有运营仓库应维护独立 `AGENTS.md`，不得依赖聊天记忆作为唯一操作依据。
- `TODO.md` 只保存可公开的短期可执行事项：`[LOCAL]` 可暂不建 Issue，`[PUBLIC]` 应转入公共 Issue。生产、客户、商业或未公开安全事项不得写入本仓库，必须进入私有系统。长期方向进入 `ROADMAP.md`，已发布变化进入 `CHANGELOG.md`，不要在 TODO 长期堆放完成日志。
- 项目原始源码采用 `AGPL-3.0-only`。修改依赖或发行内容时必须运行 `pnpm run check:licenses`，并保留 `LICENSE`、`NOTICE` 和 `THIRD_PARTY_NOTICES.md`；不能删除、改写或用自定义摘要替代 GNU 官方许可证原文。
- `CLA.md` 和 `COMMERCIAL.md` 当前不是已生效合同。法定许可主体、律师复核和可审计签署流程完成前，不得声称已经获得商业再许可权，也不得合并外部版权性贡献。商业合同、CLA 签署记录和个人身份材料只进入私有合规仓库。

## 2. 不可破坏的数据约束

- 所有写请求必须在 `BEGIN IMMEDIATE` 事务中读取最新数据、校验并提交，不得绕过 `writeStore()`。
- `transactions` 是历史事实，不得静默修改或删除。删除成员或耗材档案时保留其历史流水快照。
- 成员 `id` 是不可变内部 UUID；`username` 是可修改登录账号。不要用账号名关联流水或识别所有者。`note` 是成员主动公开的负责范围/联系方式备注。
- `groups` 是课题组、公共平台等内部组织的一对一成员归属；`tags` 是采购负责人、危化品等可多选职责。标签不能参与分组消耗相加。
- 系统所有者和系统管理员可通过 `/api/profile` 修改自己的 `groupId`；普通成员与库存管理员不能自行改变核算归属。个人资料接口不得接受角色、所有权、账号状态或登录账号等权限字段。
- 每笔流水必须保存发生时的 `groupId`、`groupName` 和 `sourceType` 快照。成员调组或组织改名不能改写历史流水；库存 Excel 差额流水使用 `inventory_adjustment`，不能计入人工分组消耗。
- `metadata.owner_user_id` 必须始终指向唯一一位 `role=admin` 且可登录的系统所有者。
- 所有权只能通过专用接口转移给启用中的系统管理员，不得通过普通角色下拉实现。
- 数据库结构变化必须提供事务迁移和旧库自动化测试；不能要求用户手工重建正式数据库。
- 密码哈希统一使用 `password.mjs`，不要在服务器、脚本和测试中复制另一套算法。

## 3. 权限模型

| 身份 | 数据库存值 | 权限边界 |
| --- | --- | --- |
| 系统所有者 | `role=admin` 且 `isOwner=true` | 唯一；完整权限；任命系统管理员；转移所有权 |
| 系统管理员 | `role=admin` | 可多人；品牌、分组、成员和完整库存管理；不能管理所有者或其他系统管理员 |
| 库存管理员 | `role=inventory` | 耗材、安全库存、导入导出和全部流水；不能管理账号、品牌或分组 |
| 普通成员 | `role=member` | 登记出入库、查看和导出完整库存及全部流水 |

权限必须以后端校验为准，前端隐藏入口只是辅助。新增接口时优先复用 `canManageInventory`、`canManageMembers`、`canManageSettings` 和 `canViewAllTransactions`，并增加越权测试。

所有启用成员都能查看“成员目录”，目录只返回显示姓名、身份、组织分组、标签和备注，不返回登录账号、最近登录或停用状态。管理员在同一份目录卡片右侧获得管理入口，并可额外看到可管理账号的登录名、状态和最近登录时间。成员本人可通过 `/api/profile` 修改姓名、备注和已有标签；管理员代改成员时继续走 `/api/users/:id` 的角色边界。目录默认按姓名的中文拼音/英文字母 A-Z 排序。

## 4. Material 3 界面规范

- 这是高频实验室工作台，保持安静、紧凑、可扫描，不做营销页或大面积装饰。
- 沿用现有颜色变量、8 px 以内内容圆角、Material 3 对话框的 28 px（手机 24 px）容器圆角、圆形图标按钮和状态层。不要引入紫色渐变、装饰光斑或卡片套卡片。
- 命令按钮使用 Lucide 图标加清楚文字；纯工具操作优先图标按钮并提供 `title` 或可访问名称。
- 所有按钮必须有 hover、focus、pressed、disabled 和异步处理中状态；沿用 `.m3-ripple`，不要添加突兀缩放动画。
- 原生 `select` 必须通过现有 `enhanceM3Select()` 统一增强，动态修改选项后调用 `refreshM3Select()`。
- 表单使用稳定的两列网格；同一行的标签和输入框顶边必须对齐，手机端降为单列。说明文字不能把相邻输入框推离基线。
- 弹窗必须有标题、关闭按钮、取消和明确的保存/确认动作；危险操作需要二次确认。
- 列表不得无说明地截断数据。固定高度区域可滚动，但要显示总数、固定表头，并提供进入完整筛选视图的入口。
- 页面 section 使用全宽或无框布局；panel 仅用于真正的数据表、工具或重复项，不创建嵌套卡片。

## 5. 响应式和可访问性

- 每次界面修改至少检查桌面和 `390 x 844` 手机视口。
- 手机抽屉打开时必须锁定背景滚动，适配浏览器动态地址栏，关闭后恢复原滚动位置。
- 手机弹窗必须以 `visualViewport` 为可见边界，同时使用 `height` 和 `offsetTop`；部分 Android 浏览器会在输入法弹出时缩短并向上平移可视视口，只使用高度会让遮罩提前结束并透出主页面。输入法打开时，弹窗底边必须紧贴输入法顶边，不保留安全区间距或透出背景内容；输入法收起后恢复底部安全间距和完整圆角。保留 `mobile-keyboard-open`、视口基准高度、顶部偏移和分阶段更新逻辑。
- 不允许文字、按钮、表格列或弹窗互相覆盖；长名称必须换行或省略并保留可查看方式。
- 键盘焦点必须可见；下拉菜单、弹窗和确认框应保持现有焦点返回逻辑。
- 不用视口宽度动态缩放字体，`letter-spacing` 保持 0（品牌英文小标签除外的现有设计不扩散）。
- 字体遵循 Material 3 的可读层级：正文、表格数据、按钮和输入控件原则上不低于 `14px`，辅助说明、标签和表头不低于 `12px`；`11px` 只用于头像首字等非正文图形。空间不足时先使用 `min-width: 0`、稳定列宽、省略或合理换行，不得继续缩小正文来硬塞。

## 6. 功能设计原则

- 当前库存的变化必须对应出入库流水。Excel 导入的是“目标当前库存”，系统生成差额流水。
- 库存统计工作簿必须包含固定的“可导入库存”工作表及六列：耗材名称、分类、当前库存、安全库存、单位、规格。导入器优先读取该表，其次兼容“逐耗材统计”，只把当前库存作为目标值，累计统计列不能回导。
- 同一用途但需要独立盘点的不同规格应建立不同耗材名称；`spec` 用于展示，不参与库存计算。
- 修改有库存耗材的单位必须拒绝，先将库存调整为 0，避免历史单位混算。
- 安全库存是逐耗材设置，`quantity <= safetyStock` 即触发预警。
- 耗材只有“使用中”和“已归档”两种档案状态。归档要求当前库存为 0；已归档耗材默认隐藏，不参与统计、预警、出入库候选和“可导入库存”，但库存管理员可筛选查看并恢复。
- 系统所有者和系统管理员可永久删除库存为 0 的耗材档案；库存管理员不能永久删除。永久删除不删除 `transactions`，旧流水继续使用保存于流水中的名称和单位快照。前后端都必须说明该操作不可恢复并二次确认。
- 归档耗材的同名入库或 Excel 回导必须拒绝并提示先恢复，不能借回导静默创建重复档案。
- 出入库字段的业务口径固定为：入库“来源（供应商）”，出库“去向（房间号 / 领用人 / 项目）”；这里的房间号是物理去向，绝不能与课题组、公共平台等内部“组织分组”混用。记录表使用“来源 / 去向”，导出另附类型列。数据库内部 `counterparty` 名称保持不变以兼容旧数据。
- 组织分组是唯一核算维度；成员标签可以多选但只用于职责说明和筛选。分组导出只统计 `sourceType=manual` 的出库，并按流水发生时的组织快照归属。
- 成员备注默认对所有启用成员可见，因此不要把密码、密钥或其他不适合组内公开的信息写入备注。
- 所有输入同时做前端体验校验和后端可信校验；后端错误要给用户可行动的中文提示。
- `/api/admin/database-backup` 和 `/api/admin/database-restore*` 仅系统所有者可用。恢复必须二次密码授权、SQLite 完整性校验、原子替换、pre-restore 快照、清空 sessions；网页恢复只适用于单个 Node/systemd 实例，不能在多个进程同时连接数据库时执行。
- `/api/bootstrap` 只携带最近 30 条流水和 `transactionTotal`，但服务器统计必须基于完整历史。记录页和全局记录搜索必须通过 `/api/transactions?mode=page` 按需分页查询；库存统计导出和流水导出必须通过显式完整导出模式取得全部流水。不得误用首屏子集或当前显示页生成不完整报表。自定义品牌图标只返回带内容哈希的 `/api/brand-icon` URL，不能重新把 Base64 塞回公开设置或首屏数据。
- 缓存必须按可变性分层：哈希 CSS/JS 和匹配内容哈希的品牌图标使用一年 `immutable`；HTML 使用 `no-cache + ETag` 允许 304；公开设置、库存、账号、流水和下载接口使用 `no-store`。品牌图标请求中的版本哈希与当前内容不符时必须返回 404，不能在旧 URL 下返回新内容。
- 耗材二维码必须绑定不可变 UUID，不能绑定可编辑名称。二维码内容使用当前站点的 `/?material=<UUID>` 直达网址，不包含账号、会话、库存数量或其他业务数据。
- 扫码只能定位耗材并打开现有登记表单，禁止扫码即增减库存；数量、类型、来源/去向、时间和备注仍由登录用户确认并经后端校验后生成流水。
- 已归档、已删除、无效和不属于当前系统的二维码必须给出明确错误。二维码生成与识别在浏览器本地完成，相关大依赖保持动态加载；摄像头只允许同源 HTTPS/localhost，HTTP/IP 必须保留图片识别和系统相机直达链接方案。

## 7. Docker 部署约束

- Docker 默认只绑定 `127.0.0.1:4388`，由部署者选择的 HTTPS 反向代理负责 `80/443`。不得假定或修改仓库外的代理配置。
- 生产容器保持单个 `app` 实例；SQLite 数据卷与备份卷必须独立持久化，禁止在日常脚本中提供或调用 `docker compose down -v`。
- 镜像使用官方 Node 24 Debian slim、多阶段构建、非 root 用户、直接 `CMD ["node", "server.mjs"]`；Compose 保留 `init`、健康检查、只读根文件系统、能力清空、资源限制和日志轮转。
- 更新流程必须先做一致性备份并保留旧镜像标签；健康检查失败不能删除卷。原生 systemd 与 Docker 迁移时只能保留一个可写实例。
- 修改 Dockerfile、Compose 或 Docker 运维脚本后，至少运行 ShellCheck、`docker compose config --quiet` 和应用回归；有 Linux Docker Engine 时还必须运行隔离的 `bash deploy/docker/openlabstock.sh smoke`，其测试项目不得连接正式卷。
- 手机端若增加 PWA，Service Worker 不得缓存 `/api/`、数据库下载或用户特定响应，也不得离线排队/重放库存写请求。

## 8. 分级验证和发布检查

完整流程和风险矩阵以 `docs/ENGINEERING_WORKFLOW.md` 为单一事实来源；本节只保留必须遵守的摘要。

验证按风险分级，避免每个小改动重复执行发布级流程：

- 日常代码改动运行 `pnpm run verify:quick`，即类型检查和回归测试；纯文档改动只检查链接、命令和受影响内容，不要求构建应用。
- 合并到主分支前由 CI 运行 `pnpm run verify`，即 `check + build + test + audit`。
- 数据库迁移、认证、权限、备份恢复、二维码、Docker 或共享库存写入等高风险改动，本地也必须运行完整 `pnpm run verify`。
- 正式发布必须先运行一次完整 `pnpm run verify`，再制作发布包并做一次独立解包启动、空库 / 升级库和健康检查。独立解包阶段只做生产烟测，不重复执行已经通过的完整测试套件；只有运行时代码或锁文件在验证后发生变化时才重新运行 `pnpm run verify`。

界面改动在日常阶段检查受影响流程；正式发布前实际登录检查桌面和 `390 x 844` 手机截图、横向溢出、弹窗对齐、下拉菜单、按钮状态和控制台错误。输入法、摄像头、PWA 等真机特有问题继续在对应设备验收，不要求无关补丁重复验证。

二维码功能修改还要按 `docs/QR_CODE_WORKFLOW.md` 验证 PNG 回扫、直达链接、归档/无效二维码、摄像头释放，以及“扫描不直接生成流水”的边界。

生产包至少包含：

- `dist/`
- `server.mjs`
- `storage.mjs`
- `password.mjs`
- `scripts/backup.mjs`
- `scripts/reset-owner-password.mjs`
- `deploy/`
- `README.md`、`DEPLOYMENT.md`、`AGENTS.md`
- `LICENSE`、`NOTICE`、`THIRD_PARTY_NOTICES.md`

升级前先执行一致性备份。切换 `/opt/openlabstock` 时不得移动、删除或解压任何文件到 `/var/lib/openlabstock`。发布后等待两秒，再检查本地和对外 HTTPS 地址的 `/api/health`。

每次制作生产包都必须先递增 `package.json` 的 `version`。发布验收时核对登录后侧栏、`curl http://127.0.0.1:4388/api/health`、对外 HTTPS 健康接口和 `/opt/openlabstock/package.json`，标准部署下四处版本必须一致；不得只根据压缩包文件名判断线上版本。
